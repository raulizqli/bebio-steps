services:
  postgres:
    image: postgres:16-alpine
    container_name: bebio_postgres
    environment:
      POSTGRES_USER: bebio
      POSTGRES_PASSWORD: bebio
      POSTGRES_DB: bebio
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
volumes:
  postgres_data:
